╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║         PyTorch 2.6 兼容性问题 - 快速修复指南                ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

问题: Weights only load failed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

您遇到的错误:
  _pickle.UnpicklingError: Weights only load failed
  Unsupported global: GLOBAL ultralytics.nn.tasks.DetectionModel

原因:
  PyTorch 2.6 改变了 torch.load() 的安全策略

修复方法 (只需1分钟!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

步骤1: 打开文件
  f:\code\trans\converter_gui.py

步骤2: 找到第431行 (搜索 "torch.load")
  找到这行:
    model = torch.load(str(pt_path), map_location=self.device.get())

步骤3: 修改为 (添加 weights_only=False):
    model = torch.load(str(pt_path), map_location=self.device.get(), weights_only=False)
                                                                       ^^^^^^^^^^^^^^^^^^^^^
                                                                       添加这个参数

步骤4: 保存文件

步骤5: 重新运行程序
  python converter_gui.py

完成! 问题解决! ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

如果使用命令行工具:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

也需要修改 convert_yolov5_to_tensorrt.py:

找到:
  model = torch.load(self.pt_model_path, map_location=self.device)

改为:
  model = torch.load(self.pt_model_path, map_location=self.device, weights_only=False)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

是否安全?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 如果您的模型来自:
  - 您自己训练的模型
  - Ultralytics官方下载
  - 可信的团队成员
  
  完全安全! 这是加载YOLOv5模型的标准方式。

❌ 如果模型来自不明来源，请谨慎!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

检查您的PyTorch版本:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

python -c "import torch; print(torch.__version__)"

如果显示 2.6.0 或更高，必须进行上述修复。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

版本更新历史:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

v1.0 - 初始版本
v1.1 - 修复TracerWarning和NameError
v1.2 - 修复PyTorch 2.6兼容性 ⭐ 当前

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

更多信息:
  - PYTORCH_2.6_FIX.md     - 详细修复指南
  - TROUBLESHOOTING.md     - 常见问题
  - CHANGELOG.md           - 完整更新历史

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
